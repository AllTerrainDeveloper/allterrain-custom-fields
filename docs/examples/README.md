# Examples

Copy-paste recipes. Each one is complete: drop it in a plugin file and it works.

| Recipe | What it shows |
|---|---|
| [`field-group-in-code.md`](field-group-in-code.md) | Declaring a field group in version control rather than in the database |
| [`bidirectional-relationship.md`](bidirectional-relationship.md) | Two post types that point at each other, kept consistent with no `save_post` hook |
| [`custom-field-type.md`](custom-field-type.md) | A field type with its own control, its own sanitiser, and something it accepts off the desktop |
| [`accept-a-drop.md`](accept-a-drop.md) | Receiving `allterrain-fields/value` in your own plugin's window |
| [`gate-a-group.md`](gate-a-group.md) | Showing a field group only to some people, which is not what location rules are for |
| [`validate-a-field.md`](validate-a-field.md) | A site rule the built-in validators cannot express |
